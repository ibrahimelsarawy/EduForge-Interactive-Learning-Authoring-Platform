'use client';
import { Bot, Check, ChevronRight, WandSparkles } from 'lucide-react';
import { useState } from 'react';
import { useModuleStore } from '@/store/module-store';
export function AICopilot() { const add = useModuleStore(s => s.addBlock); const [prompt, setPrompt] = useState('Create a practical lesson about emergency funds'); const [running, setRunning] = useState(false); const [result, setResult] = useState<string[]>([]); const run = () => { setRunning(true); setResult([]); window.setTimeout(() => { setRunning(false); setResult(['Generated a lesson outline with 4 learning moments.', 'Added a real-world scenario simulator.', 'Suggested a knowledge check and reflection prompt.']); }, 900); }; const actions = [['Generate lesson', 'richText'], ['Generate scenario', 'scenarioSimulator'], ['Generate flashcards', 'flashcards'], ['Generate knowledge check', 'knowledgeCheck']] as const; return <section className="mt-5 grid gap-5 lg:grid-cols-[1.1fr_.9fr]"><div className="rounded-3xl border bg-white p-6"><div className="flex items-center gap-3"><div className="grid h-10 w-10 place-items-center rounded-xl bg-violet-50 text-violet-600"><Bot size={19}/></div><div><h2 className="font-black">AI Course Copilot</h2><p className="text-xs text-slate-400">Assisted authoring — every change stays under your control.</p></div></div><textarea className="mt-6 min-h-36 w-full resize-none rounded-2xl border bg-slate-50 p-4 text-sm outline-none focus:border-violet-400" value={prompt} onChange={e => setPrompt(e.target.value)}/><button onClick={run} className="mt-3 inline-flex items-center gap-2 rounded-xl bg-violet-600 px-5 py-3 text-xs font-black text-white shadow-lg shadow-violet-200">{running ? <span className="animate-pulse">Thinking…</span> : <><WandSparkles size={14}/> Generate learning plan</>}</button>{result.length ? <div className="mt-5 space-y-2">{result.map(x => <div key={x} className="flex gap-2 rounded-xl bg-emerald-50 p-3 text-xs font-semibold text-emerald-800"><Check size={14}/>{x}</div>)}</div> : null}</div><div className="rounded-3xl border bg-slate-950 p-6 text-white"><div className="text-xs font-black uppercase tracking-widest text-violet-300">One-click generators</div><div className="mt-5 space-y-2">{actions.map(([label, type]) => <button key={label} onClick={() => add(type)} className="flex w-full items-center justify-between rounded-2xl border border-white/10 bg-white/5 p-4 text-left text-sm font-bold transition hover:-translate-y-0.5 hover:bg-white/10"><span>{label}</span><ChevronRight size={15}/></button>)}</div><p className="mt-5 text-[11px] leading-5 text-slate-500">Demo mode uses deterministic local generation so the portfolio works without an API key. Swap the generator service for a real model later.</p></div></section>; }

